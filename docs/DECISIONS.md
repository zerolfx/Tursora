# 决策记录

按时间顺序。每条写清选了什么、为什么、代价是什么。改主意时在原条目下追加，不删。

| # | 日期 | 决策 | 理由 / 代价 |
|---|---|---|---|
| D0 | 2026-08-19 | **不移植 Dolphin / KIO，原生重写** | Phase 0 审计（[audit/PHASE-0-REPORT.md](audit/PHASE-0-REPORT.md)）：移植可行，但基础功能在 macOS 原生 API 上几乎免费，KIO 栈几乎纯是成本；Dolphin 的差异化在 UI 行为。审计不作废，它记录的 APFS 大小写、NFD/NFC、`UF_HIDDEN`、per-volume `.Trashes` 原生路线一样会踩 |
| D1 | 2026-09-10 | 标签栏**自绘**，不用 `NSWindow.tabbingMode` | 原生窗口标签把"每标签一个 window"绑死，和分栏打架；也做不出 Dolphin 式紧凑标签。代价：标签的拖拽、重排、关闭按钮全部自己画 |
| D2 | 2026-09-10 | v1 视图 = 列表（`NSOutlineView`，可就地展开）+ 图标（`NSCollectionView`），带缩放与预览 | 原定图标视图 v2，用户要求提前。紧凑视图不做 |
| D3 | 2026-09-10 | `Return` = 重命名（Finder），`⌘↓` = 打开；点击已选中项的文字延迟进入重命名 | 跟 macOS 用户的肌肉记忆，不跟 Dolphin。判断不依赖 `mouseDown` 与拖放会话回调的先后 |
| D4 | 2026-09-10 → 2026-09-11 | 应用名 **Tursora**，bundle id `com.tursora.Tursora`（打包脚本 `TURSORA_BUNDLE_ID` 可覆盖） | 原名 Otter File Manager / `com.otterfm.Otter`，2026-09-11 改名。代价：release 版的 UserDefaults 域随 bundle id 变，旧的收藏与偏好不迁移 |
| D5 | 2026-09-11 | 分栏提前到 v1，结构照 Dolphin（标签 → 1–2 pane），不是"每 pane 多标签" | 用户确认对标 Dolphin |
| D6 | 2026-09-10 | **不用 Xcode**：SPM + 打包脚本产出 `.app`，无 nib / storyboard / asset catalog | Command Line Tools 就够；代价：所有 UI 在代码里建，图标只能用 SF Symbol 与系统图标 |
| D7 | 2026-09-10 | `swift-tools-version:5.9`，Swift 5 语言模式 | 避开 Swift 6 严格并发的整改成本；AppKit 代码大量主线程回调 |
| D8 | 2026-09-10 | 验证靠**应用内 smoke test**（`TURSORA_SMOKE_TEST=1`），不靠截图 | 开发环境拿不到屏幕访问权限；每个功能都要加 headless 可查的检查，跑三遍看稳定性。代价：外观只能由人看 |
| D9 | 2026-09-11 | 快捷键：`⌘1…9` = 标签页（Dolphin），视图模式用 `⌘⌥1/2`；`⌘L` = 编辑地址 | 与 Finder 的 `⌘1–4` 视图、`⌘L` 替身冲突，接受；对齐留作后续项（[gaps/GAP-vs-FINDER.md](gaps/GAP-vs-FINDER.md)） |
| D10 | 2026-09-11 | 文件操作语义照 Finder：拖放不弹菜单（同卷移动 / 跨卷复制 / `⌥` 复制）；冲突对话框照 Finder 的 Keep Both / Skip / Stop / Replace + Apply to all | Dolphin 的"松手弹菜单"和"全部跳过 / 覆盖 / 自动重命名"都改成 macOS 用户熟悉的形态 |
| D11 | 2026-09-11 | 过滤：形态照 Finder（工具栏搜索框 + 范围栏），语义照 Dolphin（子串 + 通配符，按 pane） | 两边各取一半，见 SPEC §8 |
| D12 | 2026-09-11 | **Finder 证据规则**：凡是"对标 Finder"的标签、措辞、图标、分组名，只用 Finder 自己的资源（`strings` 抽 nib、`plutil` 读字符串表）做依据；推断出来的要在文档里标"仍是推断" | 记忆里的 Finder 细节经常错（组名、分组顺序）；代价：nib 去重后拿不到每一项的图标名 |
| D13 | 2026-09-11 | 撤销按窗口，不做 Finder 式全局撤销栈 | `NSWindow.undoManager` 现成；Info 窗口里的改名注册到 Info 窗口自己的栈 |
| D14 | 2026-09-11 | Info 窗口永不成为 main window | 否则 Inspector 与 Go 菜单会跟着 Info 窗口走。用 `canBecomeMain = false` 子类 |
| D15 | 2026-09-11 | 读文件 flag（锁定、隐藏扩展名、可读写）走 `FileManager`，不走 `URL.resourceValues` | 后者在同一次 run loop 内缓存，写完立刻读拿到旧值 |
| D16 | 2026-09-11 | 文件夹始终排在文件前，不可关 | Finder / Dolphin 默认；可关做成偏好留作后续 |
| D17 | 2026-09-11 | 后台 agent 只用于读代码出文档、对 diff 做一次对抗性 review；难度估算这类不再大规模并行 | 用户反馈 97 个 agent 过度且撞额度 |
| D18 | 2026-09-12 | smoke test 启动等待首轮目录加载完成，最多 15 秒 | 固定等一秒会把尚未完成的异步加载当成空目录；按 generation 等待，空目录与错误也会结束等待，超时明确失败。代价：卡住的加载最多等 15 秒；不承诺冷启动目录能在一秒内列完 |
| D19 | 2026-09-12 | 移除 D11 的被动范围栏，保留名称过滤框和状态栏计数；真实递归搜索单独规划 | Dolphin 明确区分过滤与搜索；只有当前目录一个范围时，禁用的文件夹按钮制造了不存在的选择。暂不加入正则、大小写开关和搜索后端 |
| D20 | 2026-09-12 | 外窗统一圆角，侧栏和内容使用平直接缝；自定义固定在左端的侧栏按钮，隐藏重复标题 | 避免系统侧栏分区导致按钮迁移和接缝不一致；代价是放弃系统侧栏的自动覆盖布局，保留手动伸缩 |
| D21 | 2026-09-12 | Tags 与 Import from iPhone 明确不做；删掉已有 Tags 分组与标签读取 | 用户定义的产品范围，不作为待补齐差距；保留文件原有标签，不写入或删除标签元数据 |
| D22 | 2026-09-12 | 更多操作复用 Browser 文件操作；分享使用系统选择器 | 菜单始终按活动 pane 选择验证，文件操作保留撤销与刷新；系统决定可用分享服务，应用不自行发送文件 |
| D23 | 2026-09-12 | CI 构建产物与手动 Release 分离 | 普通 push / PR 只产出快照；Release 仅 main 手动触发，为确切 SHA 创建新 tag，拒绝覆盖旧 tag。目前产物为 Apple Silicon、ad-hoc 签名 |
| D24 | 2026-09-12 | ZIP 通过系统 ditto / libarchive 在隔离工作目录处理，完成后独占重命名发布 | 避免覆盖、部分输出和路径越界；保留 macOS 元数据与隔离标记。代价是压缩先复制输入、需要额外磁盘空间；本轮不支持密码及其他格式 |
| D25 | 2026-09-12 | 远程存储走系统 NetFS 挂载，浏览器继续只处理 file URL | 用户要求 Finder 式连接服务器与挂载卷；系统处理凭据和协议，应用复用本地视图和操作。没有自建 SFTP/KIO 后端；真实服务端仍需现场验证 |
| D26 | 2026-09-12 | 增加应用级设置；只开放扩展名显示、名称过滤快捷键和两个实验开关 | 用户要求可配置；过滤快捷键检查已有命令冲突，扩展名只改显示、重命名用真名。其他 Finder / Dolphin 偏好仍未实现 |
| D27 | 2026-09-12 | 终端使用固定 SwiftTerm 1.15.0 + PTY，默认关闭；导航只更新手动 Restart 的目标 | 真实交互程序需要终端仿真。向已有会话注入 cd 可能落入正在运行的命令或 shell read，所以不自动同步；代价是换目录须显式重启，关闭面板结束会话 |
| D28 | 2026-09-12 | ZIP 浏览为默认关闭的独立只读窗口，打开文件使用临时副本，不写回归档 | 保留默认 Open 即 Extract，避免把普通文件操作错误应用到虚拟归档。副本保留到应用退出供外部程序使用，需 Save As 保留修改；代价是完整暂存占磁盘、不是零解压浏览 |
| D29 | 2026-09-12 | 取代 D28 的独立窗口方案，将只读 ZIP 浏览接入现有 pane；导航使用原 ZIP 加内部路径的逻辑 URL，读取映射到共享临时副本 | 复用列表 / 图标、历史、地址栏、过滤、标签与分栏，避免两套浏览交互；写入入口统一禁用，Copy / 拖出只复制，终端使用原 ZIP 所在目录。实验仍默认关闭，关闭开关不破坏已有归档页；完整解压占空间与不回写的限制不变 |
| D30 | 2026-09-12 | 默认每目录记忆；使用应用自身 Application Support 中的版本化路径库，提供统一默认、保存当前默认与恢复当前目录。普通目录修改不实时同步其他已打开的同目录 pane，显式默认 / 策略 / 重置操作同步受影响 pane；ZIP 等逻辑页仅用当次状态 | [固定 Dolphin 源码依据](research/directory-view-properties.md)。独立保存两种视图缩放；默认与目录定制分开，恢复不写回。应用库不污染用户目录、不要求目录可写，比 xattr 更易一致处理损坏和只读卷；代价是记录不随移动或复制，挂载点或旧路径复用可能继承记录。路径先解析 symlink；alias 依赖导航解析，不用 inode/bookmark 追踪。串行合并原子写入避免迟到覆盖；未知版本保留到实际修改。与 Dolphin 不同，切换统一策略不会自动提升当前 pane 为默认。过滤 / 选择 / 滚动 / 历史不持久化；会话恢复由后续 D48 独立实现，递归应用仍另行规划 |
| D31 | 2026-09-12 | 本地复制 / 移动统一为可暂停、继续、取消的独立任务；冲突在任务内异步决定，暂存完整后才发布 | 保证单个大文件可控制、任务不互相阻塞，Replace 失败不会先删旧目标。原子移动 / 元数据收尾不虚构 ETA；不扩展网络协议或 ZIP 工具控制 |
| D32 | 2026-09-12 | 传输撤销保存可逆的文件系统改名日志与私有恢复内容，绑定任务原窗口；关浏览窗口取消并等待，关标签继续，退出取消所有任务 | 只撤销真实成功项，Replace 与跨卷 Move 可安全恢复，Merge 不动无关目标内容。代价是恢复内容在撤销历史保留期间占用空间；退出前等待清理可能受文件系统响应时间限制 |
| D33 | 2026-09-12 | 搜索独立于名称过滤；名称 / 类型 / 日期直接后台遍历，正文用 NSMetadataQuery；保存条件放应用偏好，搜索视图仅保留当次 pane 状态 | 让未索引普通目录也能查名称，正文复用 macOS importer；范围明确为当前文件夹或 Home。接受正文索引限制、不搜 ZIP、一次结果快照且不与 Finder Smart Folder 互通；结果无默认写入目标，文件操作始终使用真实 URL。搜索继承启动时的 pane 视图，不能覆盖来源目录记录或统一默认；关闭后重新读取目录设置。文件操作刷新重新执行当前查询 |
| D34 | 2026-09-12 | 地址栏归每个 pane 所有；分栏标题固定左右顺序（本条原括号标记已由 D37 取代），允许自定义名；标签栏始终显示并提供 Dolphin 的七项右键动作 | [固定源码及取舍](research/pane-paths-and-tab-actions.md)。路径同时可见、分别编辑，操作捕获所属 pane / tab，切换不提交未确认输入。始终显示比单页自动隐藏多占一行，但让分栏标题和菜单可发现。Detach 按一到两个逻辑位置重建窗口，保留活动侧、自定义名和搜索请求；不迁移历史、过滤、选区、文件任务或撤销栈，保持原窗口任务的取消与恢复边界。会话恢复见后续 D48，合并窗口仍另行规划 |
| D35 | 2026-09-12 | 保留双窗格尾鳍原画，以确定的 CoreGraphics 裁切生成透明外沿的 PNG / ICNS，不再依赖手工图标包的自动圆角假设 | 用户截图与旧图标的不透明角暴露方形边缘问题；1024 px 画布内采用 80 px 边距、864 px 底板及 192 px 圆角，原前景比例和位置不变。原画与导出分开可重复生成并验证 alpha；不新增阴影 / 描边，不宣称复刻系统遮罩，也不在本轮引入 Icon Composer / Xcode 管线。证据与验证见[图标边缘记录](research/app-icon-edges.md) |
| D36 | 2026-09-12 | 标签交互保留 Dolphin，外观主要参考 Safari 独立标签栏与胶囊形当前页，Finder 作辅助对照；多标签滚动并提供文字菜单，亮暗默认跟随系统 | 每 pane 独立路径与 Compact 地址标签不兼容；居中标题和悬停关闭减少干扰，固定新增入口避免拥挤重叠。尺寸与颜色混合由本应用选择，不声称复制私有材质；保留系统语义颜色并在 layer 更新周期重新解析，支持运行中切换。文字溢出菜单不等同 Finder 缩略图总览；[证据与验证](research/tabs-and-appearance.md) |
| D37 | 2026-09-12 | 分栏标题简化为 `Left | Right`；移除所有 Reveal in Finder 入口，不把通用标签页作为独立卖点 | 用户明确以替代 Finder 为目标。活动侧由 pane 提示线表达，不改变标题标点；搜索结果定位继续在 Tursora 内完成。产品介绍集中在双分栏、独立路径等工作流，标签完整操作保留在技术文档 |
| D38 | 2026-09-12 | Get Info 初始只展开 General 与 Preview；新显式折叠偏好与旧自动保存值分开 | 对齐本机 Finder 已观察状态，不宣称工厂默认。旧 false 保留；无法区分自动写入和主动选择的旧 true 采用新基线，新操作完整记忆。初始化不再写偏好；[迁移与证据](research/info-disclosures.md) |
| D39 | 2026-09-12 | 文本图标优先绘制开头片段，字号保持 7–10 pt，其他格式沿用系统缩略图 | 用户反馈整页缩小后文字不可辨认；参考 Dolphin 文本预览的纸面与字号策略，两种主题均保持高对比内容。后台读取 / 排版有界，缓存包含屏幕倍率，回调按请求身份验证。代价是长文本只显示开头、无语法高亮；不支持编码回退 Quick Look；[证据与验证](research/text-thumbnails.md) |
| D40 | 2026-09-12 | 共用一个工具栏输入框：默认当前目录过滤，输入后提供 Search Options 进入递归搜索；移除独立 Search 按钮，详细条件按需展开并自动执行 | 用户明确保留过滤习惯；借鉴 Dolphin 的 500 ms 防抖 / Return 立即搜索与条件入口，不复制其独立 Filter Bar 布局。查询草稿与过滤分别归 pane，避免切换或迟到输入作用到错误目录；空条件不自动扫描。正文仍查询 Spotlight 已有索引，无逐文件全文兜底；当前过滤支持 glob 而递归名称仍为文字包含，模式提示说明差别；[固定源码与边界](research/search-input-reference.md) |
| D41 | 2026-09-12 | README 使用特性 / 截图两列表格；真实截图以确定脚本移除窗口外白底，统一透明 PNG | 用户明确要求保留真实圆角与内部像素。JPEG 已丢失原始 alpha，只在边角估算透明度，内部以原生解码像素逐字节校验；亮色图只复用完全配准窗口的 alpha。歧义边缘拒绝并重拍，不画通用圆角罩；PNG 增加体积换来亮暗背景融合，网站同步使用透明背景；[管线与证据](research/screenshot-transparency.md) |
| D42 | 2026-09-12 | Dock 提供 New Window、Downloads、Applications，固定命令均新开窗口 | 给用户熟悉的 Dock 入口，不依赖当前焦点或改变已有工作区；标准 Dock 项由系统管理。独立 selector 兼容系统传入 nil sender，缺少标准目录时禁用对应项；这三个入口的组合是 Tursora 选择，不宣称复刻 Finder 完整 Dock 菜单；[证据](research/dock-menu.md) |
| D43 | 2026-09-12 | 更新采用固定 Sparkle 2.9.6；每日自动检查默认开、自动下载安装默认关，两项可在独立 Updates 设置页调整 | 复用成熟的签名校验、安装与重启流程，避免应用自行替换二进制。关闭检查保留手动入口及保存的下载偏好，不取消已下载或安排退出安装的会话；偏好由 Sparkle 持久化，smoke 注入 driver 且完全不启动网络 updater。代价是包内增加签名 helper/framework，原 0.1.0 必须先手动升级一次；[依据与边界](research/app-updates.md) |
| D44 | 2026-09-12 | 稳定 feed 随最新正式 GitHub Release 作为 `appcast.xml` 资产发布，更新 DMG 采用固定版本 URL + Ed25519 签名；build 默认使用源码提交时间戳 | 网站部署与更新发布独立，版本说明可随同一次 release 携带，prerelease 不进入稳定 feed。正式版同时检查 semver 与已有 feed build 递增，避免两个工作流 run number 或重写历史计数产生降序。签名私钥需独立保管并配置仓库 secret，不能只靠公开 checksum；本轮仍不做 Developer ID / 公证或 delta 更新 |
| D45 | 2026-09-12 | 中文产品页用官方 GitHub Pages Actions 部署静态 `site/dist/`，只允许 `main` 发布，PR 仅构建验证 | 沿用无依赖 Python 构建和真实截图；相对链接支持 `/Tursora/` 子路径，不将仓库文档树当站点发布。下载入口指向最新正式 release，网站无需随每次版本发布重建；配置和本地检查不冒称已上线，实际部署单独记录于[Pages 研究](research/github-pages.md) |
| D46 | 2026-09-12 | 按用户要求改为直接 DMG 分发，固定应用 → Applications 两图标与箭头；用 hash 固定的 dmgbuild 写入布局，不依赖 Finder 自动化 | 首次安装可直接拖拽，Sparkle 也使用同一 DMG，避免维护两套分发归档；镜像构建需额外 Python 工具，但可在独立 venv 与无 Finder CI 中重复验证。原 0.1.0 ZIP 保留。用户确认没有 Developer ID，暂不引入无法验证的 Apple 签名脚本；会员 / 证书 / 公证凭据齐备后另行接入，Mac App Store 的 sandbox 与分发设计也不混入本轮 |
| D47 | 2026-09-13 | 参考 Rascal 的下载 / 拖拽 / 首次启动顺序，以 Apple 当前 Open Anyway 流程为主，并在 README 折叠展示只移除 Tursora 隔离属性的终端备选 | 当前未公证，需要说明首次启动路径；备选要求官方来源与同版本 SHA-256 相符，不照搬清空全部扩展属性的命令，也不把损坏或恶意软件警告一概当作隔离属性。网站只给简短路径及 README 链接，没有虚构 Homebrew tap 或自动修改系统安全设置；文档说明与实机 Gatekeeper 验证分开记录 |
| D48 | 2026-09-13 | 工作区恢复默认开启，独立会话库存窗口、标签顺序 / 名称 / 选中项、双 pane 位置 / 活动侧 / 比例、侧栏与窗口几何 / 最小化；已执行搜索重跑，ZIP 存逻辑 URL；Settings 可关并清除保存 | 优先解决日常退出后丢失工作上下文。0.4 秒防抖、退出先同步保存，私有权限原子替换；坏文件 / 未知版本保留到用户 Retry Saving Workspace 或关闭再开，不因普通导航被覆盖。恢复保留缺失目录，不重连服务器；关闭 ZIP 实验时只对可识别归档退到父目录。导航历史、关闭页、过滤、选区、滚动、终端、传输和撤销不跨进程恢复，避免重放操作。16 窗口 / 每窗 32 标签及 2 MiB 限制用于有界恢复，超限保存明确失败并保留旧文件。代价是本轮只续接位置与布局；[实现与验证状态](research/workspace-sessions.md) |
| D49 | 2026-09-13 | 按用户要求默认启用终端入口与 ZIP 浏览，保留显式关闭偏好；ZIP 准备按 pane 订阅取消并提供失败重试，终端显示启动 / 报告目录与独立结束状态 | 减少首次使用门槛，终端仍需 F4 才启动。ZIP 仍完整解压，最后等待者取消进程后才清理，已供外部程序使用的副本保留到退出；关闭标签会停止准备，重开再试。退出先抓一次逻辑工作区，待传输与归档清理完成。终端不注入 cd，缺少 OSC 7 时明确只知启动目录；旧偏好键保留以兼容，旧版未记录的“关闭”无法与从未选择区分；[实现与验证](research/default-features-polish.md) |
| D50 | 2026-09-13 | 应用命令统一进入可搜索的快捷键目录，按稳定 ID 保存覆盖 / 清除，保留旧 Filter 绑定；原生文本和终端输入仍由对应视图处理 | 用户要求所有应用快捷键可定制。主菜单未绑定项也可录制，额外 Return / Space、标签定位与备用键分别列出；冲突报告归属，不静默抢占。已知系统组合保留，Control / 功能键在输入中交回原视图，终端开关例外。不把 readline、文本选择或系统快捷键混入应用目录；[范围与验证](research/custom-shortcuts.md) |
| D51 | 2026-09-13 | 终端设置分离 shell 启动配置与可即时应用的外观；工具栏提供当前窗口的展开 / 收起按钮 | 默认系统登录 shell、系统等宽 12 pt、跟随外观；自定义 shell 仅接受绝对可执行路径，下一次打开 / 重启生效。字体与前景 / 背景颜色立即更新所有已开终端且不重启 PTY。延续手动 Restart、不注入 cd、关闭终止会话的生命周期，避免设置操作中断命令；[SwiftTerm 与 Rascal 源码对比](research/terminal-customization.md) |
| D52 | 2026-09-13 | 保留 Places，在左侧下方加入按需读取的独立 Folders 树；每窗口记忆可见性 / 高度比例 / 隐藏与 Home 限制选项 | 参考 Dolphin 两种独立面板的分工，收藏负责固定入口，树负责目录层级。树跟随活动 pane、独立于文件过滤和分组；原生 NSOutlineView，模型通过 FileProvider 单层读取。默认隐藏，不预扫整盘，关闭停用监听；不实现 Dolphin 的任意停靠 / 浮动。ZIP 跟随源归档父目录，扩展节点不跨进程恢复；[源码依据与验证](research/folder-tree.md) |
| D53 | 2026-09-13 | 用户明确选择 MIT；README / 网站突出免费开源，Homebrew tap 复用当前公开仓库并固定真实发布资产 | 无需为自有 tap 新建仓库或先取得 Apple 会员；公开 main 合入后使用双参数 brew tap。真实 0.1.0 ZIP、校验和与隔离安装已验证，不虚构未发布 DMG 或官方 homebrew/cask 收录资格。cask 不移除隔离、不删用户状态；Developer ID / 公证与 Sparkle 各自独立；[分发证据](research/homebrew.md) |
| D54 | 2026-09-13 | 所有 canonical 截图的透明圆角成为网站构建门槛，补充深色边缘附近彩色系统标记的保守采样回退 | 只在原对角样本无法解释边缘时尝试本角已测得的顶边 / 侧边色，保留同一残差、对比阈值、窄边界和 protected-pixel 校验；不改窗口内部或替换系统指示。亮色或仍歧义的图继续拒绝，须原生 alpha 或精确配准参考；[初始盘点与最终截图阶段](research/screenshot-audit-2026-09-13.md) |
| D55 | 2026-09-13 | 按用户补充要求，终端的显示与进程生命周期分离：隐藏或禁用入口保留每窗口 PTY；退出、关窗和 Restart 对任务先确认 | 取代 D51 沿用的收起即终止，避免隐藏面板中断命令或丢失 shell 状态。查询前台、后台和已停止任务，状态未知也保守询问，Cancel 为默认；退出确认在保存退出状态及取消传输之前。代价是隐藏的终端仍消耗进程资源，进程检查不能等同完整 shell 作业控制；不探测 shell 输入，不恢复跨进程会话；[边界与验证](research/terminal-session-lifecycle.md) |
| D56 | 2026-09-13 | 按用户确认，终端状态放在当前标签最右 pane 的底部并可点击切换，每窗口只显示一个入口 | 隐藏终端后仍可看到会话 / 任务状态，避免左右 pane 重复。初始展示不启动 PTY；已有会话约两秒后台采样，窄栏用图标与完整提示。任务数是检测到的进程数，状态可能有采样延迟，破坏性动作仍即时检查；[范围与验证](research/terminal-session-lifecycle.md) |
| D57 | 2026-09-13 | 按维护者确认，安装说明以 0.2.0 新安装为起点，不提供无用户的 0.1.0 迁移引导；Homebrew 保留主仓库双参数 tap，并补充单 cask 信任排错 | 不让没有实际用户的历史版本占据首次安装流程；保留真实历史标签、资产与验证记录，不为文案重发二进制。显式 URL 避免自动推导不存在的 homebrew-tursora；信任按指定 cask 授予，不要求关闭全局保护或信任整个仓库；[验证依据](research/homebrew.md#安装排错仓库地址与单项信任) |
| D58 | 2026-09-13 | 按用户明确要求，Homebrew 主安装命令固定为完整 URL tap → 仅信任 Tursora cask → 完整名称 install 三步 | 将显式 `brew trust --cask zerolfx/tursora/tursora` 从 D57 的错误后备选提升为文档主流程必做步骤，减少用户遇到 untrusted tap 后再查排错；只授权此 cask，不信任整个 tap 或关闭检查。代价是比可自动信任的完整名称安装多一条命令；已验证自动信任可成功，不把文档选择说成 Homebrew 的普遍强制要求。历史两步记录保留，不改应用、版本 / build、标签或资产；[依据与阶段](research/homebrew.md#安装排错仓库地址与单项信任) |
| D59 | 2026-09-13 | 安装位置按实际可写权限选择；直接展示单应用 quarantine 命令，并提供指向 Pages 纯 Markdown 指南的 agent 指令 | 取代 D47 的固定系统 Applications 和折叠终端备选，满足无管理员账号及自选目录安装。仅删除所选可信副本的 com.apple.quarantine，不清空全部属性、不改全局策略；组织限制和 Homebrew 自身写权限另行处理。指南原样发布，agent 按环境操作，不新增远程 shell 安装器或 cask 自动移除属性的 hook；不为文档重发二进制 |
| D60 | 2026-09-13 | 取代 D27 / D49 / D51 的手动目录跟随和 D56 的底部终端状态：zsh 在安全提示符消费私有目录请求；顶部保留紧凑操作栏，底部移除全部终端入口与可用容量 | 用户要求导航时跟随，同时明确取消挤占状态栏的图标、动画与计时。路径作为私有文件中的数据，经 FIFO 唤醒 ZLE；只在空的主提示符执行内建 cd，命令、read、续行和未提交输入保留到安全提示符再处理，不发送清行按键、cd 文本或信号，不改用户启动文件。隐藏会话继续跟随；仅 zsh 自动同步，其他 shell 保留手动 Restart，不反向驱动浏览器。标题栏详情放提示 / 辅助功能；状态栏只留文件上下文、缩放与操作进度，移除终端轮询和容量查询。隐藏保留与终止前确认沿用 D55；[范围与验证](research/terminal-navigation-0.2.1.md) |
| D61 | 2026-09-13 | 取代 D45 的单一中文产品页：网站默认英文，保留独立中文页与可见语言切换 | 用户要求英文作为默认入口；`index.html` 与 `zh.html` 共用样式、脚本、截图和安装指南，使用普通链接切换，无需 JavaScript、自动检测或重定向。两页翻译产品文字、元数据、图片及辅助功能描述，保持相同章节与安装步骤；代价是后续文案须同步两份页面。应用界面仍为英文，网站双语不代表应用已本地化；静态验证、实际浏览与部署分别记录于[站点说明](../site/README.md) |
| D62 | 2026-09-13 | 冒烟套件改用共享 `SmokeSuite` 协议（统一 check / 轮询 / 夹具）与 `SmokeTest.run` 的扁平步骤表 | 34 个测试文件各自复制 check 与轮询，共 7 种签名、格式不一且带冗余 fflush；统一后新套件只声明前缀，轮询超时 / 间隔集中定义。代价是输出格式归一（个别套件的三空格、尾随空格改为统一的 ` — 详情`），检查总数以本轮 smoke 记录为准；步骤表只改写嵌套闭包，不改套件顺序与同步 / 异步区分 |
| D63 | 2026-09-13 | 删除 `ViewPreferences` 旧全局视图偏好及其首次导入 | 这些 UserDefaults 键只在 0.1.0 之前的开发构建写入，任何已发布版本都只读不写；目录视图库缺失时直接使用内置默认值，与 smoke 模式路径一致。代价是 0.1.0 前开发构建残留的全局视图偏好不再被导入；已发布版本用户不受影响。相关的 `topLevelSources` 无生产调用方，同批删除，其纯函数检查改经 `mutationSources` 覆盖 |
| D64 | 2026-09-13 | 批量重命名落盘用「两遍临时名 + `FileManager.moveItem`」，而不是单项重命名的 `URLResourceValues.name` | 实测 `URLResourceValues.name` 改成已存在的同级名称不会报错、直接覆盖目标；批量重命名必须拒绝覆盖批外文件，`moveItem` 天然拒绝（NSCocoaError 516）。临时名这一遍同时解决连锁 / 互换，并让只改大小写也走普通 move；单项重命名仍保留 `URLResourceValues` 路径。证据见[批量重命名](research/finder-batch-rename.md) |
| D65 | 2026-09-13 | 目录同步改为双向并扩展到 bash / fish：三种 shell 都从提示符发 OSC 7，反向报告驱动当前标签的活动 pane；bash 用临时 `--rcfile`、fish 用 `--init-command`，只有 zsh 保留 FIFO/ZLE 的即时路径 | 取代 D60 的单向 zsh 限定。OSC 7 是 SwiftTerm 已支持且各 shell 都能发的既有协议，比给 bash / fish 造应答通道便宜；代价是 bash / fish 只能在用户画出的下一个提示符生效、没有失败应答，bash 还变成交互非登录 shell。防回环靠四个条件（面板可见、非本面板请求、非浏览器当前目录、目录确实变了）而不是时间窗；两个方向各给一个默认开的开关，`Configuration` 改为逐字段解码以免旧偏好整体失效。范围与限制见[双向同步记录](research/terminal-shell-sync.md) |
| D66 | 2026-09-13 | 新增命令面板：⇧⌘O 打开可成为 key 的浮动面板，对全部命令 + 收藏 + 当前窗格历史目录做模糊搜索；命令经既有菜单项与响应链执行 | Finder 没有对应功能，标题 `Command Palette…` 属推断；⇧⌘P 已被 Show Previews 占用，改用空闲的 ⇧⌘O，并像其它命令一样进 `ShortcutCatalog` 以便自定义。面板必须能成为 key 才能接收输入，代价是打开后 `NSApp.targetForAction` 会走面板自己的响应链，因此 target 与可用性都在**浏览窗口**的响应链上显式解析，执行前先关闭面板再重新校验。可用状态是打开瞬间的快照，不实时刷新；不可用命令置灰列出而非隐藏。历史来自每个窗格的 `NavigationHistory`，不是应用级「最近打开」；Help 菜单的系统菜单搜索保留不动。[取证、边界与验证](research/command-palette.md) |
