# Decision Log

In chronological order. Each entry records what was chosen, why, and what it costs. When we change our mind, the new entry is appended below the original one; nothing is deleted.

| # | Date | Decision | Rationale / cost |
|---|---|---|---|
| D0 | 2026-08-19 | **No Dolphin / KIO port; a native rewrite** | Phase 0 audit ([audit/PHASE-0-REPORT.md](audit/PHASE-0-REPORT.md)): a port is feasible, but the basic functionality comes almost for free on native macOS APIs while the KIO stack is almost pure cost; Dolphin's differentiation lies in its UI behaviour. The audit is not void — the APFS case handling, NFD/NFC, `UF_HIDDEN` and per-volume `.Trashes` issues it records will be hit on the native route just the same |
| D1 | 2026-09-10 | The tab bar is **custom-drawn**, not `NSWindow.tabbingMode` | Native window tabs hard-wire "one window per tab", which fights with split panes; they also cannot produce Dolphin-style compact tabs. Cost: tab dragging, reordering and close buttons all have to be drawn by hand |
| D2 | 2026-09-10 | v1 views = list (`NSOutlineView`, expandable in place) + icons (`NSCollectionView`), with zoom and preview | The icon view was originally planned for v2; the user asked for it earlier. Compact view is not being built |
| D3 | 2026-09-10 | `Return` = rename (Finder), `⌘↓` = open; clicking the text of an already-selected item enters rename after a delay | Follows the muscle memory of macOS users, not Dolphin. The decision does not depend on the ordering of `mouseDown` and the drag-session callbacks |
| D4 | 2026-09-10 → 2026-09-11 | App name **Tursora**, bundle id `com.tursora.Tursora` (the packaging script's `TURSORA_BUNDLE_ID` can override it) | Originally Otter File Manager / `com.otterfm.Otter`, renamed on 2026-09-11. Cost: the release build's UserDefaults domain follows the bundle id, so old favourites and preferences are not migrated |
| D5 | 2026-09-11 | Split panes moved up to v1, structured like Dolphin (tab → 1–2 panes), not "several tabs per pane" | The user confirmed that we follow Dolphin |
| D6 | 2026-09-10 | **No Xcode**: SPM plus a packaging script produces the `.app`; no nib / storyboard / asset catalog | The Command Line Tools are enough. Cost: every piece of UI is built in code, and icons can only be SF Symbols and system icons |
| D7 | 2026-09-10 | `swift-tools-version:5.9`, Swift 5 language mode | Avoids the rework cost of Swift 6 strict concurrency; AppKit code is full of main-thread callbacks |
| D8 | 2026-09-10 | Verification relies on the **in-app smoke test** (`TURSORA_SMOKE_TEST=1`), not on screenshots | The development environment has no screen access permission. Every feature needs checks that can be inspected headlessly, run three times to see whether they are stable. Cost: appearance can only be judged by a human |
| D9 | 2026-09-11 | Shortcuts: `⌘1…9` = tabs (Dolphin), view modes on `⌘⌥1/2`; `⌘L` = edit the location | This conflicts with Finder's `⌘1–4` views and its `⌘L` make alias; accepted. Aligning them is left as a follow-up item ([gaps/GAP-vs-FINDER.md](gaps/GAP-vs-FINDER.md)) |
| D10 | 2026-09-11 | File-operation semantics follow Finder: drag and drop pops up no menu (move within a volume / copy across volumes / `⌥` copies); the conflict dialog follows Finder's Keep Both / Skip / Stop / Replace plus Apply to all | Dolphin's "menu on drop" and its "skip all / overwrite / auto-rename" are both reshaped into the forms macOS users know |
| D11 | 2026-09-11 | Filtering: the shape follows Finder (toolbar search field plus scope bar), the semantics follow Dolphin (substring plus wildcards, per pane) | Half from each side; see SPEC §8 |
| D12 | 2026-09-11 | **Finder evidence rule**: any label, wording, icon or group name that claims to "match Finder" is based only on Finder's own resources (`strings` to extract the nibs, `plutil` to read the string tables); anything inferred must be marked "still inferred" in the docs | Remembered Finder details are often wrong (group names, group order). Cost: after nib deduplication we cannot get the icon name of every individual item |
| D13 | 2026-09-11 | Undo is per window; no Finder-style global undo stack | `NSWindow.undoManager` is there already; a rename done in an Info window registers on the Info window's own stack |
| D14 | 2026-09-11 | An Info window never becomes the main window | Otherwise the Inspector and the Go menu would follow the Info window. Done with a `canBecomeMain = false` subclass |
| D15 | 2026-09-11 | Reading file flags (locked, hidden extension, readable/writable) goes through `FileManager`, not `URL.resourceValues` | The latter caches within a single run loop pass, so a read straight after a write returns the old value |
| D16 | 2026-09-11 | Folders always sort before files, and this cannot be turned off | Finder / Dolphin default; making it a preference is left as a follow-up |
| D17 | 2026-09-11 | Background agents are used only to read code and produce docs, and to do one adversarial review of a diff; things like difficulty estimation are no longer run massively in parallel | User feedback: 97 agents was excessive and hit the quota |
| D18 | 2026-09-12 | The smoke test waits at startup for the first directory load to finish, up to 15 seconds | A fixed one-second wait treats an asynchronous load that has not finished as an empty directory; waiting by generation ends the wait for empty directories and errors too, and a timeout fails explicitly. Cost: a stuck load is waited on for up to 15 seconds; we do not promise that a cold-start directory can be listed within one second |
| D19 | 2026-09-12 | Remove D11's passive scope bar, keep the name filter field and the status-bar count; real recursive search is planned separately | Dolphin clearly separates filtering from search; with only the current directory as a scope, the disabled folder button manufactured a choice that does not exist. Regular expressions, a case switch and a search backend are not added for now |
| D20 | 2026-09-12 | A uniform corner radius on the outer window, with a flat seam between sidebar and content; a custom sidebar button pinned at the left end, and the duplicate title hidden | Avoids the button migration and the inconsistent seam caused by the system sidebar's own split; the cost is giving up the system sidebar's automatic overlay layout, keeping manual resizing |
| D21 | 2026-09-12 | Tags and Import from iPhone are explicitly not being built; the existing Tags group and the tag reading are deleted | Product scope defined by the user, not counted as a gap to close. A file's existing tags are preserved: we neither write nor delete tag metadata |
| D22 | 2026-09-12 | More-actions reuse the Browser file operations; sharing uses the system picker | Menus always validate against the selection of the active pane, and file operations keep undo and refresh; the system decides which share services are available, and the app never sends files itself |
| D23 | 2026-09-12 | CI build artifacts are kept separate from manual Releases | An ordinary push / PR only produces a snapshot; a Release is triggered manually on main only, creates a new tag for an exact SHA, and refuses to overwrite an old tag. The artifact is currently Apple Silicon, ad-hoc signed |
| D24 | 2026-09-12 | ZIP is handled through the system ditto / libarchive in an isolated working directory, and published by an exclusive rename once finished | Avoids overwrites, partial output and path escapes; preserves macOS metadata and the quarantine flag. The cost is that compression copies the input first and needs extra disk space; this round supports neither passwords nor other formats |
| D25 | 2026-09-12 | Remote storage goes through system NetFS mounts; the browser continues to handle only file URLs | The user asked for Finder-style Connect to Server and mounted volumes; the system handles credentials and protocols, and the app reuses the local views and operations. There is no home-grown SFTP/KIO backend; a real server still needs on-site verification |
| D26 | 2026-09-12 | Add app-level settings; expose only extension display, the name-filter shortcut and two experimental switches | The user asked for configurability; the filter shortcut is checked against existing command conflicts, and the extension setting only changes display — renaming uses the real name. Other Finder / Dolphin preferences are still not implemented |
| D27 | 2026-09-12 | The terminal uses a pinned SwiftTerm 1.15.0 plus a PTY, off by default; navigation only updates the target of a manual Restart | Real interactive programs need terminal emulation. Injecting a cd into an existing session could land inside a running command or a shell read, so we do not sync automatically; the cost is that changing directory requires an explicit restart, and closing the panel ends the session |
| D28 | 2026-09-12 | ZIP browsing is a separate read-only window, off by default; opening a file uses a temporary copy and does not write back to the archive | Keeps the default Open = Extract and avoids applying ordinary file operations to a virtual archive by mistake. The copy is kept until the app quits so external programs can use it, and keeping changes requires Save As; the cost is that a full staging copy takes disk space and that this is not zero-extraction browsing |
| D29 | 2026-09-12 | Replaces D28's separate-window design: read-only ZIP browsing is wired into the existing pane, and navigation uses a logical URL made of the original ZIP plus the internal path, with reads mapped onto a shared temporary copy | Reuses the list / icon views, history, the location bar, filtering, tabs and split panes, so there are not two sets of browsing interactions; every write entry point is uniformly disabled, Copy / dragging out only copies, and the terminal uses the directory containing the original ZIP. The experiment is still off by default, and turning the switch off does not break an already-open archive tab; the limits of full extraction taking space and of not writing back are unchanged |
| D30 | 2026-09-12 | Per-directory memory by default; a versioned path library in the app's own Application Support, offering a uniform default, saving the current settings as the default, and restoring the current directory. An ordinary directory change is not synced live to other open panes showing the same directory, while explicit default / policy / reset actions do sync the affected panes; logical tabs such as ZIP use only the state of the current session | [Pinned Dolphin source evidence](research/directory-view-properties.md). The two view modes store their zoom separately; the default and the per-directory customisation are kept apart, and restoring does not write back. An app-owned library does not pollute user directories and does not require the directory to be writable, and it handles corruption and read-only volumes more consistently than xattr; the cost is that a record does not follow a move or a copy, and that a mount point or a reused old path may inherit a record. Paths resolve symlinks first; aliases rely on navigation to resolve them, with no inode/bookmark tracking. Serialised merging with atomic writes avoids a late write overwriting a newer one; an unknown version is preserved until an actual modification. Unlike Dolphin, switching to the uniform policy does not automatically promote the current pane to be the default. Filter / selection / scroll / history are not persisted; session restore is implemented separately in the later D48, and recursive application is still planned separately |
| D31 | 2026-09-12 | Local copy / move are unified into independent tasks that can be paused, resumed and cancelled; conflicts are decided asynchronously inside the task, and the result is published only once staging is complete | Guarantees that a single large file stays controllable and that tasks do not block one another, and that a failed Replace never deletes the old target first. The atomic move / metadata finalisation do not invent an ETA; this does not extend to network protocols or to controlling the ZIP tools |
| D32 | 2026-09-12 | Transfer undo stores a reversible filesystem rename journal plus private restore content, bound to the task's originating window; closing a browser window cancels and waits, closing a tab lets it continue, and quitting cancels all tasks | Only genuinely successful items are undone; Replace and cross-volume Move can be restored safely, and Merge does not touch unrelated content at the target. The cost is that the restore content occupies space for as long as the undo history keeps it, and that waiting for cleanup before quitting may be limited by filesystem response time |
| D33 | 2026-09-12 | Search is independent of the name filter; name / kind / date traverse in the background directly, while content uses NSMetadataQuery; saved criteria live in the app preferences, and the search view keeps only the current session's pane state | Lets an unindexed ordinary directory still be searched by name, while content reuses the macOS importers; the scope is explicitly the current folder or Home. We accept the limits of content indexing, do not search inside ZIPs, take a one-shot snapshot of the results and do not interoperate with Finder Smart Folders; results have no default write target, and file operations always use the real URL. Search inherits the pane view as it was at launch, and cannot overwrite the source directory's record or the uniform default; after it closes, the directory settings are read again. A file-operation refresh re-runs the current query |
| D34 | 2026-09-12 | The location bar belongs to each pane; split-pane titles have a fixed left/right order (the parenthetical marker originally in this entry has been superseded by D37) and allow a custom name; the tab bar is always shown and offers Dolphin's seven context-menu actions | [Pinned source and trade-offs](research/pane-paths-and-tab-actions.md). Both paths are visible at once and edited separately, an action captures the pane / tab it belongs to, and switching does not commit uncommitted input. Always showing the tab bar costs one more row than auto-hiding it for a single tab, but it makes the split-pane titles and the menu discoverable. Detach rebuilds the window from one or two logical locations, preserving the active side, custom names and the search request; it does not migrate history, filters, selection, file tasks or the undo stack, which keeps the cancel and restore boundaries of the original window's tasks intact. For session restore see the later D48; merging windows is still planned separately |
| D35 | 2026-09-12 | Keep the dual-pane tail-fin artwork, and generate PNG / ICNS with transparent outer edges through a deterministic CoreGraphics crop, no longer relying on a hand-made icon pack's assumption of automatic rounded corners | The user's screenshot and the old icon's opaque corners exposed a square-edge problem; within a 1024 px canvas we use an 80 px margin, an 864 px plate and a 192 px corner radius, leaving the foreground's proportion and position unchanged. Keeping the artwork and the export separate makes generation repeatable and the alpha verifiable; we add no shadow / stroke, do not claim to reproduce the system mask, and do not bring in the Icon Composer / Xcode pipeline this round. For evidence and verification see the [icon edge record](research/app-icon-edges.md) |
| D36 | 2026-09-12 | Tab interaction stays with Dolphin, while the appearance mainly follows Safari's separate tab bar and capsule-shaped current tab, with Finder as a secondary reference; many tabs scroll and get a text menu, and light / dark follows the system by default | Per-pane independent paths are incompatible with Compact address tabs; a centred title and a close button on hover reduce distraction, and a fixed new-tab entry point avoids crowding and overlap. The sizes and the colour blending are this app's own choice, and we do not claim to copy private materials; system semantic colours are kept and re-resolved during the layer update cycle, which supports switching at runtime. The text overflow menu is not the equivalent of Finder's thumbnail overview; [evidence and verification](research/tabs-and-appearance.md) |
| D37 | 2026-09-12 | Split-pane titles simplified to `Left \| Right`; every Reveal in Finder entry point is removed, and generic tabs are not presented as a standalone selling point | The user's explicit goal is to replace Finder. The active side is expressed by the pane indicator line, without changing the title's punctuation; locating a search result continues to happen inside Tursora. The product description concentrates on workflows such as the dual split panes and independent paths, while the full set of tab actions stays in the technical docs |
| D38 | 2026-09-12 | Get Info initially expands only General and Preview; the new explicit-disclosure preference is kept separate from the old auto-saved values | Matches the state observed in Finder on this machine; we do not claim it is the factory default. Old false values are kept; an old true value, which cannot be told apart from an automatic write or a deliberate choice, takes the new baseline, and new actions are remembered in full. Initialisation no longer writes preferences; [migration and evidence](research/info-disclosures.md) |
| D39 | 2026-09-12 | Text icons draw the opening fragment first, keeping the font size at 7–10 pt; other formats keep using the system thumbnails | User feedback: the text is unreadable once a whole page is shrunk down; following Dolphin's paper-and-font-size strategy for text previews, the content stays high-contrast in both themes. Background reading / layout is bounded, the cache key includes the screen scale factor, and callbacks are validated against the request identity. The cost is that long text shows only its beginning and has no syntax highlighting; there is no encoding fallback to Quick Look; [evidence and verification](research/text-thumbnails.md) |
| D40 | 2026-09-12 | A single shared toolbar input field: it filters the current directory by default, and once you type, Search Options leads into recursive search; the separate Search button is removed, and the detailed criteria expand on demand and run automatically | The user explicitly wanted to keep the filtering habit; we borrow Dolphin's 500 ms debounce / Return-searches-immediately and its criteria entry point, without copying its separate Filter Bar layout. The query draft and the filter each belong to a pane, so that switching or a late keystroke cannot act on the wrong directory; empty criteria never trigger an automatic scan. Content still queries Spotlight's existing index, with no per-file full-text fallback; the current filter supports globs while recursive name matching is still literal containment, and the mode hint explains the difference; [pinned source and boundaries](research/search-input-reference.md) |
| D41 | 2026-09-12 | The README uses a two-column feature / screenshot table; on the real screenshots the white background outside the window is removed by a deterministic script, producing uniformly transparent PNGs | The user explicitly asked to keep the real corner radius and the interior pixels. JPEG has already lost the original alpha, so transparency is only estimated at the corners while the interior is verified byte for byte against natively decoded pixels; light-mode images only reuse the alpha of a perfectly registered window. An ambiguous edge is rejected and re-shot rather than covered with a generic rounded-corner mask; PNG trades extra size for blending into both light and dark backgrounds, and the website uses the same transparent background; [pipeline and evidence](research/screenshot-transparency.md) |
| D42 | 2026-09-12 | The Dock menu offers New Window, Downloads and Applications, and all of these fixed commands open a new window | Gives users a familiar Dock entry point without depending on the current focus or changing an existing workspace; the standard Dock items are managed by the system. Dedicated selectors tolerate the nil sender the system passes in, and an item is disabled when its standard directory is missing; the combination of these three entries is Tursora's own choice, and we do not claim to reproduce Finder's complete Dock menu; [evidence](research/dock-menu.md) |
| D43 | 2026-09-12 | Updates use a pinned Sparkle 2.9.6; the daily automatic check is on by default and automatic download and install is off by default, both adjustable on a dedicated Updates settings page | Reuses a mature signature-check, install and relaunch flow instead of letting the app replace its own binary. Turning the check off keeps the manual entry point and the saved download preference, and does not cancel a session that has already downloaded or scheduled an install on quit; the preferences are persisted by Sparkle, and the smoke test injects a driver and never starts the network updater at all. The cost is a signed helper/framework added to the bundle, and that the original 0.1.0 must be upgraded manually once first; [rationale and boundaries](research/app-updates.md) |
| D44 | 2026-09-12 | The stable feed is published with the latest official GitHub Release as an `appcast.xml` asset, the update DMG uses a version-pinned URL plus an Ed25519 signature, and the build number defaults to the source commit timestamp | Website deployment and update publishing stay independent, the release notes can ride along with the same release, and a prerelease never enters the stable feed. An official release checks both semver and that the build number increases against the existing feed, avoiding a descending sequence produced by two workflows' run numbers or by a rewritten history's count. The signing private key must be kept separately and configured as a repository secret; a public checksum alone is not enough. This round still does not do Developer ID / notarisation or delta updates |
| D45 | 2026-09-12 | The Chinese product page deploys the static `site/dist/` through the official GitHub Pages Actions, publishes only from `main`, and PRs only verify the build | Keeps the dependency-free Python build and the real screenshots; relative links support the `/Tursora/` sub-path, and the repository's doc tree is not published as the site. The download entry point points at the latest official release, so the site does not have to be rebuilt for every version release; the configuration and the local checks do not pretend the site is live, and the actual deployment is recorded separately in the [Pages research](research/github-pages.md) |
| D46 | 2026-09-12 | At the user's request, distribution changes to a direct DMG with a fixed app → Applications pair of icons and an arrow; the layout is written by a hash-pinned dmgbuild rather than by Finder automation | A first install can be a straight drag, and Sparkle uses the same DMG, so we avoid maintaining two distribution archives; building the image needs an extra Python tool, but it can be verified repeatedly in an isolated venv and in CI without Finder. The original 0.1.0 ZIP is kept. The user confirmed there is no Developer ID, so for now we do not add an Apple signing script we cannot verify; it will be wired in separately once the membership / certificate / notarisation credentials are in place, and the Mac App Store's sandbox and distribution design are not mixed into this round either |
| D47 | 2026-09-13 | Follow Rascal's download / drag / first-launch ordering, lead with Apple's current Open Anyway flow, and show in a collapsed section of the README a terminal alternative that removes the quarantine attribute from Tursora only | We are not notarised at the moment, so the first-launch path has to be explained; the alternative requires an official source and a SHA-256 matching the same version, does not copy the command that strips all extended attributes, and does not treat every "damaged" or malware warning as a quarantine attribute. The website gives only the short path plus a link to the README, invents no Homebrew tap and changes no system security settings automatically; the documentation and the on-device Gatekeeper verification are recorded separately |
| D48 | 2026-09-13 | Workspace restore is on by default; a separate session store holds windows, tab order / names / selected items, dual-pane locations / active side / ratio, and the sidebar plus window geometry / miniaturisation; an executed search is re-run, and ZIP stores its logical URL; Settings can turn it off and clear what was saved | The priority is to stop the everyday loss of working context after quitting. A 0.4 second debounce, a synchronous save before quitting, and an atomic replace with private permissions; a bad file / unknown version is preserved until the user does Retry Saving Workspace or closes and reopens, and is never overwritten by ordinary navigation. Restore keeps a missing directory and does not reconnect to servers; when the ZIP experiment is turned off, only recognisable archives fall back to the parent directory. Navigation history, closed tabs, filters, selection, scroll position, the terminal, transfers and undo are not restored across processes, so that no operation is replayed. The limits of 16 windows / 32 tabs per window and 2 MiB keep restore bounded; a save beyond those limits fails explicitly and keeps the old file. The cost is that this round only carries over locations and layout; [implementation and verification status](research/workspace-sessions.md) |
| D49 | 2026-09-13 | At the user's request the terminal entry point and ZIP browsing are enabled by default, with an explicit off preference kept; ZIP preparation is cancelled by per-pane subscription and offers a retry on failure, and the terminal shows the launch / reported directory and a separate exit status | Lowers the barrier to first use; the terminal still needs F4 to start. ZIP still extracts in full, cleanup happens only once the last waiter has cancelled the process, and a copy already handed to an external program is kept until quit; closing a tab stops the preparation, and reopening tries again. On quit we take one snapshot of the logical workspace, then wait for the transfers and the archive cleanup to finish. The terminal does not inject cd, and where OSC 7 is missing it states plainly that only the launch directory is known; the old preference key is kept for compatibility, and an "off" that an older version never recorded cannot be told apart from never having chosen; [implementation and verification](research/default-features-polish.md) |
| D50 | 2026-09-13 | All app commands go into one searchable shortcut catalog, overrides / clears are stored by a stable ID, and the old Filter binding is kept; native text and terminal input are still handled by the corresponding views | The user asked for every app shortcut to be customisable. Unbound main-menu items can be recorded too, and the extra Return / Space, the tab navigation and the alternate keys are listed separately; a conflict is reported together with the command that owns it, never silently taken over. Known system combinations are left alone, and Control / function keys inside a text input are handed back to that view, with the terminal toggle as the exception. readline, text selection and system shortcuts are not mixed into the app catalog; [scope and verification](research/custom-shortcuts.md) |
| D51 | 2026-09-13 | Terminal settings separate the shell launch configuration from the appearance that applies immediately; the toolbar offers an expand / collapse button for the current window | The defaults are the system login shell, the system monospaced font at 12 pt, and following the system appearance; a custom shell accepts only an absolute executable path and takes effect on the next open / restart. The font and the foreground / background colours update every open terminal immediately and without restarting the PTY. It continues the lifecycle of a manual Restart, no cd injection and closing ending the session, so that a settings change never interrupts a command; [SwiftTerm and Rascal source comparison](research/terminal-customization.md) |
| D52 | 2026-09-13 | Keep Places and add below it on the left a separate Folders tree that reads on demand; visibility / height ratio / the hidden-items and limit-to-Home options are remembered per window | Follows the division of labour between Dolphin's two separate panels: favourites provide the fixed entry points, the tree provides the directory hierarchy. The tree follows the active pane and is independent of the file filter and grouping; it is a native NSOutlineView whose model reads one level at a time through FileProvider. It is hidden by default, does not pre-scan a whole disk, and stops watching when closed; Dolphin's arbitrary docking / floating is not implemented. ZIP follows the parent directory of the source archive, and expanded nodes are not restored across processes; [source evidence and verification](research/folder-tree.md) |
| D53 | 2026-09-13 | The user explicitly chose MIT; the README / website highlight that it is free and open source, and the Homebrew tap reuses the current public repository and pins real published assets | No need to create a new repository for our own tap or to obtain an Apple membership first; once merged into the public main, a two-argument brew tap is used. The real 0.1.0 ZIP, its checksum and a quarantined install have been verified; we do not invent an unpublished DMG or eligibility for the official homebrew/cask index. The cask does not remove quarantine and does not delete user state; Developer ID / notarisation and Sparkle stay independent of each other; [distribution evidence](research/homebrew.md) |
| D54 | 2026-09-13 | Transparent rounded corners on every canonical screenshot become a gate for the website build, with a conservative sampling fallback added for coloured system markers near a dark edge | The top-edge / side-edge colour already measured for that corner is tried only when the original diagonal sample cannot explain the edge, and the same residual, contrast threshold, narrow boundary and protected-pixel checks are kept; nothing inside the window is changed and no system indicator is replaced. Light-mode or still-ambiguous images continue to be rejected and require native alpha or a precisely registered reference; [initial inventory and final screenshot stage](research/screenshot-audit-2026-09-13.md) |
| D55 | 2026-09-13 | At the user's additional request, the terminal's visibility and its process lifecycle are separated: hiding or disabling the entry point keeps the per-window PTY; quitting, closing a window and Restart confirm first when tasks are running | Replaces the collapse-means-terminate behaviour carried over from D51, so that hiding the panel never interrupts a command or loses shell state. We query foreground, background and stopped jobs, ask conservatively even when the state is unknown, and make Cancel the default; the quit confirmation comes before saving the exit state and cancelling transfers. The cost is that a hidden terminal still consumes process resources and that a process check is not the same thing as full shell job control; we do not probe shell input and do not restore sessions across processes; [boundaries and verification](research/terminal-session-lifecycle.md) |
| D56 | 2026-09-13 | As confirmed by the user, the terminal status sits at the bottom of the rightmost pane of the current tab and can be clicked to toggle, with only one entry point shown per window | The session / task status stays visible after the terminal is hidden, without being duplicated in the left and right panes. Showing it initially does not start a PTY; an existing session is sampled in the background about every two seconds, and a narrow bar uses an icon plus a full tooltip. The task count is the number of processes detected and the status may lag behind the sampling, but a destructive action still checks immediately; [scope and verification](research/terminal-session-lifecycle.md) |
| D57 | 2026-09-13 | As confirmed by the maintainer, the install instructions start from a fresh 0.2.0 install and provide no migration guide for a 0.1.0 that has no users; Homebrew keeps the two-argument tap of the main repository, plus troubleshooting for trusting a single cask | Keeps a historical version with no actual users out of the first-install flow; the real historical tags, assets and verification records are kept, and no binary is re-released for the sake of wording. An explicit URL avoids automatically deriving a homebrew-tursora that does not exist; trust is granted for the named cask, and we do not require disabling global protection or trusting the whole repository; [verification evidence](research/homebrew.md#installation-troubleshooting-repository-address-and-single-item-trust) |
| D58 | 2026-09-13 | At the user's explicit request, the main Homebrew install command is fixed as three steps: full-URL tap → trust the Tursora cask only → install by full name | This promotes the explicit `brew trust --cask zerolfx/tursora/tursora` from D57's on-error fallback to a mandatory step of the documented main flow, so that users do not have to go looking for troubleshooting after running into an untrusted tap; it authorises only this cask and neither trusts the whole tap nor disables the check. The cost is one command more than an install by full name that can be trusted automatically; automatic trust has been verified to work, and we do not present a documentation choice as a universal Homebrew requirement. The historical two-step record is kept, and the app, the version / build, the tags and the assets are unchanged; [rationale and stage](research/homebrew.md#installation-troubleshooting-repository-address-and-single-item-trust) |
| D59 | 2026-09-13 | The install location is chosen by actual write permission; the single-app quarantine command is shown directly, and an agent instruction pointing at the plain-Markdown guide on Pages is provided | Replaces D47's fixed system Applications folder and its collapsed terminal alternative, so that installs without an administrator account and into a directory of one's own choosing are possible. Only com.apple.quarantine on the chosen, trusted copy is removed; all attributes are not cleared and no global policy is changed; organisational restrictions and Homebrew's own write permissions are handled separately. The guide is published as it stands and the agent acts according to the environment; we add neither a remote shell installer nor a cask hook that removes the attribute automatically; no binary is re-released for the sake of the docs |
| D60 | 2026-09-13 | Replaces the manual directory following of D27 / D49 / D51 and the bottom terminal status of D56: zsh consumes a private directory request at a safe prompt; a compact action bar is kept at the top, and every terminal entry point and the available capacity are removed from the bottom | The user asked for the terminal to follow navigation, and at the same time explicitly cancelled the icons, animation and timers that crowded the status bar. The path is data in a private file, and ZLE is woken through a FIFO; the built-in cd runs only at an empty primary prompt, while commands, read, continuation lines and uncommitted input are held until a safe prompt, and we send no line-clearing keystrokes, no cd text and no signals, and change no user startup files. A hidden session keeps following; only zsh syncs automatically, other shells keep the manual Restart, and nothing drives the browser in reverse. Title-bar details move into the tooltip / accessibility description; the status bar keeps only the file context, the zoom and the operation progress, and the terminal polling and the capacity query are removed. Keeping the session when hidden and confirming before terminating follow D55; [scope and verification](research/terminal-navigation-0.2.1.md) |
| D61 | 2026-09-13 | Replaces D45's single Chinese product page: the website is English by default, with a separate Chinese page and a visible language switch | The user asked for English as the default entry point; `index.html` and `zh.html` share the styles, scripts, screenshots and installation guide, and switch through ordinary links, with no JavaScript, automatic detection or redirect. Both pages translate the product copy, the metadata, the images and the accessibility descriptions, and keep the same sections and installation steps; the cost is that future copy has to be kept in sync across two pages. The app interface is still English, and a bilingual website does not mean the app is localised; static verification, actually browsing the site and deployment are recorded separately in the [site notes](../site/README.md) |
| D62 | 2026-09-13 | The smoke suites move to a shared `SmokeSuite` protocol (unified check / polling / fixtures) and a flat step table in `SmokeTest.run` | 34 test files each copied their own check and polling, 7 signatures in all, inconsistently formatted and carrying a redundant fflush; after the unification a new suite only declares its prefix, and the polling timeout / interval are defined in one place. The cost is that the output format is normalised (the three spaces and the trailing spaces of a few suites become the uniform ` — <detail>`), and that the total check count is whatever this round's smoke record says; the step table only rewrites the nested closures and changes neither the order of the suites nor the synchronous / asynchronous distinction |
| D63 | 2026-09-13 | Delete the old global view preferences in `ViewPreferences` and their first-run import | These UserDefaults keys were only written by development builds from before 0.1.0; every released version only reads them and never writes them. When the directory view library is missing, the built-in defaults are used directly, which matches the smoke-mode path. The cost is that global view preferences left over from a pre-0.1.0 development build are no longer imported; users of released versions are unaffected. The related `topLevelSources` has no production caller and is deleted in the same batch, its pure-function check now being covered through `mutationSources` |
| D64 | 2026-09-13 | Batch rename writes to disk with "a two-pass temporary name plus `FileManager.moveItem`" rather than the `URLResourceValues.name` used by single-item rename | Measured: setting `URLResourceValues.name` to a sibling name that already exists raises no error and simply overwrites the target; a batch rename must refuse to overwrite a file outside the batch, and `moveItem` refuses it naturally (NSCocoaError 516). The temporary-name pass also resolves chains / swaps, and lets a case-only change go through an ordinary move as well; single-item rename keeps the `URLResourceValues` path. For evidence see [batch rename](research/finder-batch-rename.md) |
| D65 | 2026-09-13 | Directory sync becomes bidirectional and extends to bash / fish: all three shells emit OSC 7 from the prompt, and the reverse report drives the active pane of the current tab; bash uses a temporary `--rcfile` and fish uses `--init-command`, while only zsh keeps the immediate FIFO/ZLE path | Replaces D60's one-way, zsh-only restriction. OSC 7 is an existing protocol that SwiftTerm already supports and that every shell can emit, which is cheaper than building a reply channel for bash / fish; the cost is that in bash / fish it can only take effect at the next prompt the user draws and there is no failure reply, and that bash additionally becomes an interactive non-login shell. Loops are prevented by four conditions (the panel is visible, the request did not come from this panel, the path is not the browser's current directory, and the directory really changed) rather than by a time window; each direction gets its own switch, on by default, and `Configuration` moves to per-field decoding so that an old preference cannot invalidate the whole thing. For scope and limits see the [bidirectional sync record](research/terminal-shell-sync.md) |
| D66 | 2026-09-13 | Add a command palette: ⇧⌘O opens a floating panel that can become key and fuzzy-searches all commands plus favourites plus the current pane's history directories; commands run through the existing menu items and the responder chain | Finder has no equivalent feature, so the title `Command Palette…` is inferred; ⇧⌘P is already taken by Show Previews, so we use the free ⇧⌘O (⇧⌘P has since moved again, to the preview pane, D82 — still not available to the palette), and like every other command it goes into `ShortcutCatalog` so that it can be customised. The panel has to be able to become key in order to receive input, and the cost is that once it is open `NSApp.targetForAction` walks the panel's own responder chain, so both the target and the availability are resolved explicitly on the **browsing window's** responder chain, and the panel is closed and the command re-validated before it runs. Availability is a snapshot taken the moment the palette opens and is not refreshed live; an unavailable command is listed greyed out rather than hidden. The history comes from each pane's `NavigationHistory`, not from an app-level "recently opened"; the Help menu's system menu search is left untouched. [Evidence, boundaries and verification](research/command-palette.md) |
| D67 | 2026-09-13 | The sort keys fill in Finder's `Date Created` / `Date Added` / `Date Last Opened`, and for all three, items with "no date" sort to the bottom in both ascending and descending order; `Date Modified` keeps its old `.distantPast` semantics | The labels and the selectors come from `ArrangeByMenu.nib`, the rawValue matches the identically named key in `GroupKey`, and old configuration files, decoded leniently by rawValue, are unaffected. "No date sorts last" is the same kind of rule as "folders always first": it is direction-independent, because reversing the order should not push items with no date to the very top. The cost is that within one list `Date Modified` and the three new keys treat `nil` differently; changing an existing key would also change already-released behaviour and existing checks, so it is left alone. [Evidence and boundaries](research/sort-columns-folder-sizes.md) |
| D68 | 2026-09-13 | The columns of the detail view become a selectable set, ticked from the header's context menu and persisted per directory alongside the other view properties; column widths are not persisted | The column titles and their order come from "Show Columns:" in `ViewOptionsWindow.nib`; we implement the six of them for which we have data, leaving Version / Comments / Tags on the gap list. `DirectoryViewProperties.listColumns` stores an array of rawValues and deduplicates and sorts it in `normalized`, so that the same set is equal regardless of order; unknown identifiers are kept at the model layer and filtered out by the view. When `NSTableHeaderView`'s `menu` is empty a right-click falls through to the file context menu, so it is assigned explicitly. Column widths are not persisted, which keeps this round's scope smaller |
| D69 | 2026-09-13 | The Size column for folders shows the item count by default (Finder's "N items" wording), and shows recursive byte counts only once `Calculate all sizes` is on; the calculation runs on a background serial queue and is cancelled by generation | With the option off Finder shows `--`; we show the item count — a deliberate difference: `--` is purely empty information, while the item count needs only one directory read. The wording comes from Finder's string table `I_ITEMS_*`, and `Calculate all sizes` is the original wording from `ViewOptionsWindow.nib`. The cache key is (standardised path, modification date); a change deep in a subtree does not alter an ancestor's mtime, so we additionally listen to `DirectoryChanges` and drop the changed directory together with all of its already-measured ancestors. Changing directory and entering search results increment the generation, and results that arrive late are simply discarded. ZIP and search results only count items. Costs: a recursive size is logical bytes rather than disk usage; the item count excludes hidden items while the byte count includes them; each instance has only one serial queue |
| D70 | 2026-09-13 | Add `.generic` to the drag source mask and treat `sourceMask == .generic` as "move, and move across volumes too"; during validation, answer AppKit with `.generic` rather than `.move` | AppKit narrows the source mask according to the modifier keys, and the previous `[.copy, .move]` narrowed a ⌘ drag down to an empty mask, so every drop was refused. The `NSDragging.h` on this machine documents only the operation enum values and `ignoreModifierKeysForDraggingSession:`; it does **not** document the ⌥/⌃/⌘ mapping, so the implementation only widens "what the source offers" instead of reproducing the narrowing logic: if some macOS version maps them differently, the symptom is that ⌘ degrades into an ordinary drag rather than performing the wrong file operation. The validation method has to answer within the narrowed mask, so `.move` is reported as `.generic`; `dropFiles` performs a move for everything that is not `.copy`, so the actual behaviour is unchanged. A read-only source still offers only `.copy`. [Evidence and boundaries](research/drag-and-drop.md) |
| D71 | 2026-09-13 | Spring loading only declares `NSSpringLoadingDestination`, leaving the delay and the switch to the system; views only report and only the controller navigates; Finder's "spring back after springing open" is not implemented | Finder itself uses this protocol, the switch and the delay live in the global domain as `com.apple.springing.enabled` / `com.apple.springing.delay`, and a home-grown timer would only be inconsistent with the system preference. The three view classes declare the protocol in an extension without adding stored properties, and find the `SpringLoadingHost` through their own dataSource/delegate, which preserves "views never touch the filesystem". The spring-open condition reuses the drop rules and additionally excludes files, read-only panels, archives, the dragged item itself and the folder it is already in; the folder tree expands in place instead of navigating the panel. The timer belongs to the system, so headless verification goes through the same activation entry point the timer uses. [Evidence and boundaries](research/drag-and-drop.md) |
| D72 | 2026-09-13 | Put Back uses our own JSON journal (`Application Support/Tursora/TrashOrigins.json`) and does not parse `.DS_Store`; an item with no entry is greyed out with the reason explained | macOS writes Finder's put-back path into private records inside the Trash's `.DS_Store`; the format is undocumented and there is no stable API. `FileOperations.trash` already returns the `(original, trashed)` pair, so recording it in our own journal is the cheapest option. The cost is that **items moved to the Trash by Finder, a shell or another app cannot be put back**; they can only be greyed out with a reason given. Writes go through a serial queue plus an atomic replace, a read failure or a version mismatch leaves the existing file untouched, and there is a cap of 5000 entries. Undoing a Put Back moves the item back to its **original path inside the Trash** and restores the entry, rather than calling `FileOperations.trash` again. [Evidence and verification](research/trash.md) |
| D73 | 2026-09-13 | The Trash does not reuse `isReadOnly`; instead `FileViewing` gets a separate `allowsRenaming`, and path comparison uses `realpath(3)` rather than `URL.resolvingSymlinksInPath()` | `isReadOnly` would force a drag to degrade into a copy, but dragging an item out of the Trash has to follow the same-volume move rule, so renaming gets its own gate, implemented in both file views. On paths, measurement shows that Foundation deliberately does not resolve `/tmp`, `/var` or `/etc`, and only rewrites the components that **already exist**, so an item's journal key changes the moment it is moved away. We now call `realpath` on the deepest existing ancestor and rejoin the remaining components. The cost is that the journal records canonical paths in the `/private/var/...` form |
| D74 | 2026-09-13 | `Empty Trash…` has no key equivalent bound by default; we do not adopt Finder's ⇧⌘⌫ | Measured on this machine (a standalone AppKit program, see the [Trash record](research/trash.md)): `NSMenu.performKeyEquivalent` ignores Shift for key equivalents such as `\u{8}` (⌫) — a menu item bound to ⇧⌘⌫ responds to a plain ⌘⌫, and an item bound to ⌘⌫ responds to ⇧⌘⌫ as well; `⌥` is unaffected and is distinguished correctly. So with our menu ordering, Finder's ⇧⌘⌫ would only ever hit the earlier Move to Trash, and once a user clears the Move to Trash binding, ⌘⌫ would trigger an **irreversible** empty. The cost is losing Finder key consistency for this one item; the command is still in the shortcut catalog, and users can assign one themselves. We do not rewrite the global matching logic inside `ShortcutMenu`, so as not to change the dispatch path of every menu for the sake of one command |
| D75 | 2026-09-14 | The shipping version is written once, in a `VERSION` file at the repository root; the README names no version at all, and the site pages and installation guide carry a `{{VERSION}}` placeholder that `site/build.py` substitutes | The number used to be repeated in about twenty places across the README, both product pages and the installation guide, and it drifted: the spec still called 0.2.0 the published release two releases later, which the English rewrite caught. Publishing now edits `VERSION` and the changelog section, nothing else. The README cannot hold a placeholder because GitHub renders it from source, so it links to `releases/latest` and to the `Tursora-<version>-macOS-arm64.dmg` asset by pattern instead. `app/tools/test_version_source.py` fails if the current version reappears in that prose, if `make-app.sh` stops reading the file, or if `VERSION` has no dated changelog section; dependency versions and references to past releases are deliberately untouched. The Release workflow still wins over the file through `TURSORA_VERSION`, so a dispatch cannot be contradicted by a stale checkout |
| D76 | 2026-09-14 | Folders lead only when sorting by Name; under every other key they take part in the sort like files. Supersedes D16 | D16 kept folders first under every key, justified as the "Finder / Dolphin default". That reading of Finder was wrong: `PreferencesWindow.nib` offers "Keep folders on top:" with the single option "In windows when sorting by name", so Finder never keeps them on top for Date Modified, Size or Kind, and `_FXSortFoldersFirst` does not exist in this machine's `com.apple.finder` domain at all. Sorting by Date Modified therefore left folders stranded in a block at the top, which is what a user reported. Name keeps the old behaviour, where it is what people expect and what the Finder option describes. Making the name case switchable is still a follow-up, as D16 said. The regression check builds a folder whose modification date falls between two files, so a folders-first comparator cannot produce the expected order |
| D77 | 2026-09-14 | A terminal transfer row hides its progress bar and its current-item line | A completed copy kept a full blue bar and the name of the last file it had copied, which reads as work still in flight; a bar means progress. The outcome is already carried by the state label, the byte total and the completed/skipped counts, all of which stay. Applies to every terminal state, not just completed: a failed or partial task keeps its byte line and its errors, and a bar there would suggest it is still going. Pause and Cancel were already hidden once terminal |
| D78 | 2026-09-14 | A split tab's two names are separate text fields divided by a hairline the strip draws down the full height of the tab, not one string containing a `\|` character. `TabTitle` carries the halves; `plain` joins them for the Rename Tab sheet and the All Tabs menu | The character sat on the text baseline, took part in middle truncation (so it was the first thing lost in a narrow tab) and could not be told apart from a folder whose own name contains a bar. A drawn rule is chrome: it stays put, it is not a letterform, and each half now truncates on its own, so a short name is no longer shortened beside a long one. The owner reviewed three options — a text-height rule, a split glyph, and a rule with the inactive side dimmed — and chose the rule, asking that it run the height of the tab; it is drawn from the top of the pill's outline to the bottom, which also keeps it distinct from the 0.5 pt by 12 pt rail that separates two tabs. The cost is a second `NSTextField` per tab and a title type that is no longer a `String`, which every call site had to acknowledge; `TabBarView.reload` keeps a plain-string overload so checks and callers that never split are untouched. A label must also be framed wider than its glyphs: a truncating `NSTextField` cell clips before the text reaches the frame edge, so sizing each half to `ceil(glyphWidth)` put an ellipsis in every split name — the exact fault this decision removes. The shortfall is 8 pt on this machine and `intrinsicContentSize` reports only half of it, so the strip measures a real cell once at startup instead of hardcoding either number. VoiceOver reads the two names separated by a comma, since it would read nothing at all for the rule |
| D79 | 2026-09-14 | A tab settles at 240 pt and New Tab follows the tabs when they do not fill the strip. The cap is soft: a title that needs more room grows the tab up to 480 pt, bounded by an equal share of the strip; every tab still shares one width | A single tab used to stretch to 320 pt and every other count filled the strip edge to edge, which left New Tab marooned at the far right of an almost empty bar — the owner called it badly inhuman. Browsers stop growing tabs well short of the window and park the control beside them, so we do the same. The cap is soft because the owner asked for it: with room to spare and a long split title, showing the whole name beats honouring a round number. One uniform width is kept deliberately, because `frameForTab` and `targetIndex(atDocumentX:)` divide by it and a per-tab width would make drag reordering and hit testing non-linear. The measurement comes from the view, so `TabStripLayout` stays pure and takes the widest natural title as a parameter; unmeasured callers get the preferred width, which is what the pure geometry checks assert |
| D80 | 2026-09-15 | Batch rename's `Name Format:` collapses from Finder's three kinds to `Number` and `Date`. `Number` takes KIO's placeholder: one run of `#` is replaced in place and the run's length sets the leading zeros. Where the placeholder is invalid we always append the number, which KIO does only when the items share an extension | Finder's `Name and Index` and `Name and Counter` are the same rule twice — a number after the name, differing only in padding — and neither can put the number anywhere else. One run of `#` expresses both (`name #` and `name #####`) and adds free placement and any padding width, so the owner's instruction to keep only the stronger of the two is satisfied by deleting nothing a user could do before. `Date` survives because a placeholder cannot express a timestamp and Name and Date shipped in 0.3.0; removing it would be a regression, not a simplification. Dolphin itself has no batch rename to copy — `DolphinView::renameSelectedItems` hands multi-selection to `KIO::RenameFileDialog` — so the semantics come from KIO's `batchrenamejob.cpp`, not from the Dolphin checkout, where grepping for it finds nothing. The deliberate deviation is the invalid-placeholder fallback: KIO applies the pattern unchanged when extensions differ, which gives every item the same name and guarantees a collision, so we append instead — an applicable plan beats a faithful one the user cannot use, and the sheet shows every resulting name before anything is renamed. KIO previews one read-only line; our full-list preview with per-name reasons is kept |
| D81 | 2026-09-15 | Markdown in the preview pane is rendered by a styling pass we own, on top of `AttributedString(markdown:)`, and follows the system appearance rather than staying light | The SDK parser is worth using — it covers CommonMark plus GitHub tables, strikethrough and autolinks with no new dependency, and the project has taken none — but it emits **semantics only**. `# Head\n\npara\n\n- a\n- b` comes back as the single string `Headparaab`: no newlines, no fonts, no paragraph styles, no colour. Every block separator, heading size, list marker, indent, code face and table is synthesised in `MarkdownRenderer`, which is a pure `String -> NSAttributedString` function so it is checked headlessly before any view exists. Raw block HTML — a `<div>` or `<img>`, common in real READMEs — arrives with a **nil** presentation intent, so it is given a block of its own rather than being force-unwrapped or glued to the paragraph above. On colour, D33 keeps rendered document content light for the paper look of a 64 pt file icon; a full-height pane is a different proposition, and a slab of white beside a dark file view is wrong, so the pane uses the dynamic label colours and follows the theme |
| D82 | 2026-09-15 | The preview pane takes ⇧⌘P, matching Finder; the thumbnail toggle that held it moves to ⌃⌘P | Finder opens its preview pane with ⇧⌘P and users arrive expecting that. Tursora had given it to Show Previews, which switches icon thumbnails on and off — a rarer, less reachable-for command. Trading them puts the common command on the familiar key. Bindings are derived from the menu, so the shortcut catalogue and the customisation UI follow automatically, and a user who had already rebound either keeps their own choice because the store only supplies a default where the user set none. The cost is that a long-time user's muscle memory for ⇧⌘P now opens a pane instead of toggling thumbnails |
| D83 | 2026-09-15 | The preview pane is a third split item appended to the sidebar/tabs split view, owned by the window; Markdown tables force the text view onto TextKit 1 | Three placements were possible. Inside the tab would follow the split-pane layout, but `TabPage` hard-codes exactly two arranged subviews and its geometry maths assumes it. On the content split view, where the terminal lives, would renumber dividers that `toggleTerminal` addresses as `ofDividerAt: 0`. Appending to the sidebar/tabs split view avoids both; it must be **appended**, never inserted, because the sidebar is looked up as `splitViewItems[0]` in three places, and a check pins that. One pane per window matches the terminal and the Folders tree, which are also per-window rather than per-tab. On layout: `NSTextTableBlock` is the only way to give a Markdown table columns, and it is ignored under TextKit 2 — the cells stack with no error, no warning, and text that still reads correctly in a log, so nothing but a geometry check would catch it. `MarkdownTextView` touches `layoutManager` at construction to force TextKit 1, and a check asserts both that it did and that a table lays out wider than one stacked column |
| D84 | 2026-09-15 | Column view is a third `FileViewing` conformer built on `NSBrowser`; selecting in a column never navigates the pane; the binding is ⌥⌘3 | `NSBrowser` is what Finder's own column view is (`TColumnView : NSBrowser`, ColumnView.nib), so it gets column scrolling, resizing, type-select and keyboard movement from AppKit and follows Finder's geometry — a 100 pt minimum, six visible columns and a 24 pt row from `ColumnView.nib`, and a 245 pt default width from the `com.apple.finder` defaults. Two things bit during the build and shaped the code. `NSBrowser` decides item mode versus the legacy matrix mode from its delegate, and `setRowHeight:` throws in matrix mode; the pane wires a view — including its icon size — before it mounts it, so the delegate is attached at construction, not in `loadView`. The exception was swallowed by HIToolbox and hung the suite rather than failing it, which is why the suite now has no headless path that can throw there. And `item(atRow:inColumn:)` hands a row straight to the delegate with no bounds check, so `child:ofItem:` returns the root sentinel for a bad index instead of trapping — a file manager must not crash because AppKit probed a row past the end. On navigation: Finder makes the deepest selected folder the location. Here every navigation re-applies the destination folder's remembered view mode, so a child folder remembered as icons would tear the columns down mid-chain; keeping the location on the column root avoids that and keeps the six readers of `currentURL` — title, path bar, sidebar, terminal, session, preview — coherent. The cost is the difference from Finder, stated in the spec. ⌘3 belongs to tab 3, so columns take ⌥⌘3 beside ⌥⌘1 and ⌥⌘2. One more contract surfaced by the column view's Escape path: `NSResponder` declares `cancelOperation:` without implementing it, so the pane's long-standing `super.cancelOperation` was an unrecognized selector waiting for a responder chain that reached it — the list and icon editors had always intercepted Escape first. Both sites now pass Escape on with `tryToPerform`, which calls only an implementer, and the column browser cancels an edit by aborting the field editor's owning control. A navigation is a fresh start: the browser remembers which folder its columns belong to, because an ancestor's listing still contains the chain the user just left and re-selecting into it by path would reopen that chain with a folder selected nobody clicked. Deselecting differs too: in the list, `deselectAll` clears every row and keeps folders expanded, but in a column browser an open column *is* the selected folder row before it, so deselecting clears only a selected file and leaves folder rows in place — otherwise every deselect would close one column, and the pane deselects more than once around a reload |
| D85 | 2026-09-16 | A content search chooses its source: `Using: Spotlight Index` or `Scan Files`, defaulting to the index. Scanning reads the files in the same walk the name search already does, bounded to 1 MB per file, 64 MB per candidate and 20,000 files | The index cannot say whether a folder is indexed. An unindexed external drive and a folder with nothing matching both return zero results, and `NSMetadataQuery` offers no signal to tell them apart — `start() == false` means the metadata service is unreachable, not that the folder is unindexed. So a fallback can only be unconditional, user-chosen, or driven by a probe the app does not have. The owner chose user-chosen, which is also what Dolphin exposes: a `Search using:` radio group reading `File Indexing` / `Simple search`, where the non-indexed path is the default whenever the location is not indexed (`Search::DolphinQuery::switchToPreferredSearchTool`). Tursora keeps the index as the default instead, so an existing saved search behaves exactly as it did. Scanning reuses the existing background walk rather than adding a second walker, so cancellation, batching, the symlink and package rules and the error reporting are already correct; what it adds is bounds, because the 50,000-result cap counts matches and can never fire for a word that is not there. Reading is the `TextThumbnailRenderer` recipe — non-blocking open, `fstat` before the first read — because a search walks whatever the user points it at, and one FIFO in the tree would otherwise block the search forever. A byte-order mark is checked before the NUL test, or every UTF-16 document would be called binary and skipped in silence. A match past the 1 MB cap is reported as truncated rather than as a miss. The costs: scanning is slower than the index on a large indexed tree, it reads only text, and the choice is one more control in the panel |
| D86 | 2026-09-16 | Settings offers to make Tursora the application macOS opens folders with, and asks LaunchServices through `NSWorkspace.setDefaultApplication(at:toOpen:)` rather than writing `NSFileViewer` and an `LSHandlers` entry itself. Nothing is cached; the role is read back on every appearance and compared by bundle identifier | macOS has no user-facing "default file manager" the way it has a default browser and mail client. What it does have is the handler for `public.folder`, which `open .` in a terminal, a folder dropped on the Dock icon and any application that asks the system to open a folder URL all resolve through, so that is the role to claim. It is not everything a user might expect: another application's "Reveal in Finder" calls `NSWorkspace.activateFileViewerSelecting`, which names Finder rather than asking for the folder type, and no handler setting redirects it. Two ways to claim it exist. The long-standing one, which other file managers have used, is to write `NSFileViewer` into the global domain and add an `LSHandlers` entry to `com.apple.LaunchServices/com.apple.launchservices.secure.plist` — undocumented, silently rewritten by the system, and a change to how the user's machine opens every folder made without the user seeing anything. The supported call has existed since macOS 12 and puts macOS's own confirmation in front of the user, so the change is theirs to approve and reversible in the same place any other default is. That confirmation is also why the completion reports what the system decided rather than what was requested, and why no check in the suite ever claims the role: it would need a human to dismiss a system alert, and it would change the machine running the tests. What is checked is the wording, the enabled state and the identity rule. Identity compares bundle identifiers, not URLs, because the same application has a different path from a build folder, a disk image and /Applications, and a path comparison would tell a user who is the default that they are not. Nothing is cached because the user can hand the role to another application at any time, from that application or from Finder's Get Info, and a remembered answer would then be a lie on a settings page. The costs: the button only works from a packaged bundle, so a debug build reports that it is not an application; and the status line names whatever holds the role without being able to say whether the user or an installer put it there |
| D87 | 2026-09-19 | With the Terminal panel switched off in Settings, its toolbar button is **removed** from the toolbar rather than dimmed, and the whole Terminal settings pane goes read-only | A permanently grey button is a question the user cannot answer from the toolbar: it says an action exists and is unavailable, without saying why or for how long. The owner found one in the shipping build and asked for the button to be gone instead. The menu already agreed — `MainMenu.applyPreferences` hides View → Show Terminal on the same preference — so the toolbar was the odd one out, and the two now say the same thing. The button is withdrawn with `NSToolbar.removeItem` and put back with `insertItem` after Split View, so `toolbarAllowedItemIdentifiers` must keep naming it even while it is absent (an identifier the delegate does not allow cannot be inserted) while `toolbarDefaultItemIdentifiers` omits it, which is what a window opened while the panel is off builds from. `syncTerminalToolbar` splits in two for this: the delegate's item factory calls only the appearance half, because the presence half runs inside `insertItem` and would recurse. The Settings pane follows the same reasoning one level down: its controls would have written a shell, a font and a colour scheme into a panel the user cannot reach, so every one of them is disabled and a line says which option turns them back on. Enabled state now has a single owner, `applyAvailability`, which applies each control's own rule — a custom shell, a custom colour scheme — and then the panel switch above it; the shell-path rule reads the popup rather than the saved configuration, so a path typed but not yet applied survives the pane being switched off and on. The costs: the toolbar layout shifts by one button when the preference changes, which a user watching both at once will see; and a preference that had been merely invisible is now the only route back, so the General text and the pane's own line have to carry the whole explanation |
| D88 | 2026-09-22 | New Folder opens the new folder's name for inline editing, deferred until the pane's listing settles; a listing reload captures an open rename and re-opens it afterwards instead of letting it commit; a name filter that would hide the new folder is cleared first | Finder does this, and the claim is now sourced from Finder's own binary rather than from memory: `setPendingNodesToSelect:startEditing:runNewFolderAnimation:renameOp:` carries a `startEditing:` flag through the same pending-selection machinery Tursora calls `pendingSelection`, and each view family has its own `startEditing…` entry point ([record](research/finder-new-folder-rename.md)). The edit has to be deferred because creating a folder provokes three listings inside about half a second — the explicit reload, the `DirectoryChanges` broadcast returning to the same pane, and the pane's own FSEvents watcher — and each one drops the views that own the field editor. Finder defers for the same reason; its selectors say so (`selectNodesNowOrLater…`, `startEditingNodeNowOrLater:…`, `afterDelay:`). Three designs were possible for surviving those reloads. Deferring every listing refresh while a rename is open was rejected: it is a repo-wide behaviour change whose failure mode — a pane stuck on a stale listing because an editor was left open — is worse than the problem. Simply **aborting** the edit on every reload, generalising the `cancelResultRename` that both views already applied to streamed search results, was tried and rejected on evidence: it makes a half-typed commit impossible, but it also throws the user's typing away whenever an unrelated change lands mid-rename, and it made the existing click-to-rename check fail one run in three. What ships instead **captures** the edit — the URL, the typed text and the selection — ends it without committing, and re-opens it on the item's new row once the reload and the pane's own `select(urls:)` have both finished, which is why the restore is deferred to the next run-loop pass rather than done inline. A half-typed name can no longer commit, and nothing the user typed is lost. The attempt is pushed back by each arriving listing and capped at five tries, so a folder in a directory under continuous external change still becomes editable rather than deferring forever. The filter rule has no Finder answer — Finder has no filter bar — and the alternative was a folder created invisibly with nothing to type into. Costs: the editor appears about a third of a second after the folder does, rather than instantly; a rename caught by a reload flickers, because the editor is genuinely closed and re-opened a run-loop pass later, and it is abandoned outright if the item it was editing has left the listing; and clearing the filter discards a filter the user set. Undo New Folder, which Finder does register (`NF1` / `NF2`), is still not implemented and is recorded in the roadmap rather than built here |
| D89 | 2026-09-22 | Return keeps Finder's rename meaning by default; the Windows / Dolphin "Return opens" convention is offered as an unbound, rebindable File View action (`file.open`, "Open Selection (Alternative)") rather than a Settings toggle or a relaxation of the bare-Return rule | D3 chose Finder's mapping — Return renames, ⌘↓ opens — and that default is unchanged. What was missing was any way to swap it: `syntaxError(context:)` accepts a bare Return only for a `.fileView` action, and the only Open command was the main-menu item, an `.application` action, so the combination was unreachable. Two other designs were rejected. Relaxing the rule so `menu.openSelection` may take a bare Return cannot be contained: `MainMenu.applyPreferences` writes a recorded binding onto the real `NSMenuItem.keyEquivalent`, and `NSMenu.performKeyEquivalent` matches it inside `sendEvent:` — app-wide, in the Settings window, the Info window and any sheet, outside the dispatcher's focus gate, which shields only `NSTextView` / `NSTextField` / `TerminalView`. A "Return opens the selected item" Settings toggle was rejected because D50 put every key behaviour in one searchable catalogue; a toggle would be the only exception, would not surface when a user searches "Open" or "Return", and would hard-code exactly one alternative. The new row ships unbound so nothing changes for existing users, which costs a user who wants it two steps: clear Rename's Return, then record Return on Open. Rebound, it dispatches to the same `openSelection()` as ⌘↓ and a double-click, with no extra gates — a bare key must not mean something different from the menu command it mirrors. That is a deliberate acceptance of three consequences, all of which ⌘↓ already has: one keypress opens every item of a multi-selection; Return enters a trashed folder; and Return on a ZIP extracts it when in-pane ZIP browsing is off, which registers undo and posts a directory change. `handleFileView` learned the same action so an inactive pane's file view, which the window-level dispatcher declines to serve, does not keep renaming on a key the user rebound. Catalogue order is the tie-break: `renameID` is registered first, so a hand-edited override map holding Return on both still resolves to Rename |
| D90 | 2026-09-22 | ZIP Extract becomes a file-operation task — one row per archive, determinate progress, Cancel but no Pause — and its progress is derived from bsdtar's own `tar -tvf` listing plus its `-v` extraction stream, not from a ZIP parser of our own | Right-click Extract showed a 16 pt spinner and could not be cancelled at all: the public `extract` never passed the `cancellation` its tool runner already accepted. Three facts, measured rather than assumed ([record](research/archive-extraction-progress.md)), made real progress cheap. `tar -x -v` writes `x <name>` to **stderr**, which the tool runner already redirects to a regular file — deliberately, so a full pipe cannot deadlock `waitUntilExit()` — so polling that file adds no pipe and keeps the property. `tar -tvf` gives an exact total up front, and on the fixtures it matched bytes-on-disk to the byte. And an entry is announced when it is **opened**, not finished, so the credited figure is every announced entry but the last plus the current file's size on disk, read through `FileManager` because `URL.resourceValues` caches for the run-loop pass and the number would stop moving. A ZIP central-directory reader of our own was rejected: progress is only meaningful if the listing agrees with what the extractor writes, bsdtar's own listing agrees by construction (including its `__MACOSX` AppleDouble folding, which `unzip -Z1` reports differently), and any disagreement yields a bar that never reaches 100%. A narrow `ArchiveListing` protocol is defined anyway so a future native reader can take its place. Pause was dropped: `NSTask.h` annotates `interrupt` and `terminate` with "Not always possible", and a paused extraction pins the whole `.tursora-archive-<uuid>` staging tree inside the user's own folder. Two parsing traps are handled explicitly and covered by pure checks, because both would have shipped a broken bar: a symbolic link's listing line carries a ` -> target` suffix that the extraction stream does not, so the kind is read from the mode character and only then is the suffix stripped — never on a " -> " substring, since `a -> b.txt` is a legal filename; and a traversal refusal is reported on a line that also begins with `x `, so progress lines are filtered by whole known name or the error message itself would be deleted. Costs: the figure is uncompressed bytes written rather than archive bytes read, so its rate and estimate are not comparable with a copy's, and the spec says so; stderr now carries progress and must be filtered before it can be shown as an error; progress is entry-granular, so one huge member inside an archive still advances in one step; and a selection of N archives puts N rows in File Operations, which is the owner's choice, taken because per-archive progress is honest and a merged row hides which archive is slow |
| D91 | 2026-09-22 | Opening a ZIP for browsing is pre-flighted before any storage is created: the local header's encryption flag refuses a password-protected archive by name, the listing seam throws a reason instead of returning an empty array, the uncompressed total is checked against free space, and storage left by a crashed run is swept at launch under an advisory lock | Groundwork for lazy browsing, and each part fixes something already wrong. **Encryption**: `tar -tvf` exits 0 on an encrypted ZIP and returns a complete, correct listing, and `tar -x` then exits 1 *while leaving a correctly-sized, entirely zero-filled file at the right path* (measured). `FileItem.readableContentURL` tests only `fileExists`, so under any design that materializes lazily those zeros would be served to Quick Look, `NSWorkspace`, drag-out and Copy. An 8-byte read of bit 0 of the general-purpose flag settles it before a directory exists; a trial extraction or a ZIP parser would both cost more and decide later. **Listing errors**: the seam returned `[]` for a damaged archive, a non-zero exit and an over-cap listing alike. For extraction progress that reads as "no total" and is harmless; for browsing it reads as "this ZIP is empty", which is a lie. It now throws `damaged` — carrying the tool's own words, which required routing the listing's stderr to a regular file rather than `/dev/null`, never a pipe — or `tooManyEntries`. The cap also moved from bytes to entries (500 000), because 8 MiB was an opaque proxy for roughly 111 000 entries at the measured 75.6 bytes each, and the parser now consumes complete lines from a chunked read so a truncated final line can never become a half-built entry. **Space**: staging goes into the user's temporary directory, so a large archive can fill the volume the system runs from, and there was no guard at all. The listing supplies the uncompressed total for one central-directory read — 0.05 s against 0.57 s to extract the same archive — so the check is affordable on every open. **Sweep**: a crash left its storage behind forever. Age is not a usable discriminator, because a session open for hours never touches its own directory, and `$TMPDIR` is per-user rather than per-process, so a debug build or a smoke run has live storage sitting beside the orphans. Ownership is therefore advisory-locked: a live session holds an exclusive `flock` on a marker beside its published root for as long as it exists, and the sweep removes only what it can lock itself. The kernel releases that lock when a process dies, which is exactly the case being cleaned up. Costs: one extra `tar -tvf` per archive opened, an 8-byte read, and a file descriptor held per open session |
| D92 | 2026-09-22 | Browsing a ZIP mounts it instead of expanding it: the table of contents becomes a directory skeleton, the symbolic links are written at mount, and a directory's own files are materialized in one batch when that directory is listed. Lazy **per directory**, not per entry | The old design expanded the whole archive before a single row appeared, and the roadmap had deferred fixing it on reasoning that measurement disproved. What the measurements actually say: `tar -tvf` costs 0.049 s where extracting the same archive costs 0.566 s, and 0.256 s against 5.873 s at 50 000 entries; per-invocation cost is linear in the archive's total entry count and independent of where the member sits, because bsdtar seeks through the central directory and every invocation pays one parse; and batching is nearly free, about 1 ms per extra member. Those three give the crossover: **about twelve entries**. Opening a dozen files one at a time already costs more tool time than staging the archive once, so true per-entry materialization is a disk win that becomes a time loss as soon as the user opens anything. Per directory keeps both. It also avoids the entire downstream cascade, which is the deeper reason: `FileItem.readableContentURL` is a synchronous, non-throwing property read from row drawing and from drag start, `canAccess` gates `open(_:)` before any trigger could fire, and the three pasteboard writers are synchronous — none of which has to change, because by the time a row exists its bytes are on disk. Symbolic links are materialized at **mount** rather than lazily, because `entries(in:)` derives `canAccess` from `validatedURL` on the real link and an escaping link is otherwise indistinguishable from a safe one. `-n` is used for a leaf and never for a directory (a ZIP need store no directory record, and with `-n` such a member is "Not found in archive"); `-q` is never used at all, because it stops at the first match and silently truncates a subtree. Success is the tool's exit status, never `fileExists` (D91). Path rewrites bsdtar performs — a stripped leading `/`, a stripped drive letter, dropped `.` components — are reproduced in the tree so a listed row resolves to the path extraction will really write. A behaviour change falls out: an archive containing a `..` entry used to refuse to open at all, because the whole-archive extraction exited non-zero; now it opens and every other entry browses. The refused entry itself is not shown — a listing reads the disk, and bsdtar never writes it there. Costs: a directory with tens of thousands of files in it still pays for all of them on entry, which is the case that would justify a later per-entry stage; the mount pays one `tar -tvf` plus a skeleton of empty directories (0.048 s per 1 000); and `entries(in:)` now has a side effect, which is why materialization lives there rather than in the caller — the alternative left every caller to remember, and the first one that forgot was found by an existing test |
| D93 | 2026-09-22 | Archive rows take their modification date from the ZIP's own central directory, read by a small reader of our own and joined onto the tree by path; an unentered archive folder's item count comes from the tree | Two regressions in D92 made this necessary before it shipped. The directory skeleton is created with `mkdir`, so every folder inside an archive showed *the moment the archive was opened* as its date — and `ditto`, and so Finder's Compress, does record directories with their own dates, so the most common archives were affected, along with date sorting and date grouping. And `FolderSizes` counts a folder by reading it from disk, where an unentered archive folder holds only its skeleton, so it read "0 items". **Dates**: a central-directory reader is acceptable here where it was rejected as the *listing* source (D90, D92), because it is used only for a join: a miss leaves a row without a date, never with a wrong row. It must agree with the extractor, or a date shown before an entry is materialized would change the moment it is, so its precedence mirrors libarchive's exactly — the DOS date and time first, in local time, then any Unix timestamp field overriding it: `0x5455` (extended timestamp), `0x5855` (Info-ZIP Unix, which `ditto` writes — without it every Finder-made archive would fall back to two-second DOS time) and `0x000d` (PKWARE Unix). NTFS `0x000a` is deliberately not read, because libarchive does not read it either. `tar -tvf`'s own date column could not substitute: it shows hours and minutes only within ±182 days of now, the year alone outside that, and never the seconds (measured). Agreement is checked directly, not assumed: for a Finder-made archive the reader's date for every entry equals, to the second, what a full extraction writes to disk. A directory the archive never recorded — synthesized from its children's paths, as a Python-written ZIP requires — has no date of its own, and keeps showing when it was opened, which is exactly what the old full extraction showed for it. **Counts**: the tree already knows what a directory's listing will contain, so the count is taken from it, filtered the same way the listing and Finder's "N items" are — extractable children, no dotfiles. Both checks were confirmed to fail on the unfixed code (the date read as the open time, the count as "0 items") before being trusted. Costs: one read of the central directory per archive opened, bounded at 64 MB, and a second parser of ZIP structure in the codebase, which is why it is confined to dates |
| D94 | 2026-09-23 | An encrypted member is found per record in the central directory and never extracted; a package's descendants are never created on their own; a directory's batch is recorded as done only when it succeeds | Three Stage 1 defects, found by the Stage 2 design pass. **Mixed encryption**: D91's pre-flight reads only the *first* local header, so an archive that starts plain and holds an encrypted member later got past it — and entering that folder left the encrypted member as a correctly-sized, zero-filled file that listed as readable and was served to Quick Look, Open and Copy. Reproduced exactly with an archive built by Info-ZIP's own `zip`, and the check was run against the unfixed code first, where it found the six zero bytes. The central directory records general-purpose bit 0 for every member, so the reader now reports it and the tree marks such a member inert: it is never put in a batch, and so never produces debris. Reading the flag per record rather than attempting extraction and cleaning up after it keeps the rule from D91 intact — success is the tool's exit status, never `fileExists`. **Package shells**: package status is decided per path by extension, so `Demo.app` is a package and `Demo.app/Contents` is not; the skeleton therefore created `Demo.app/Contents`, and a package not yet extracted already existed on disk as an empty shell. A node now knows when it is inside a package, and such nodes are left out of both the skeleton and the mount-time links. A package is extracted whole, less any encrypted member inside it, which is passed to bsdtar as an `--exclude` (measured: with a member list, `--exclude` works on either side of `-T` and leaves nothing behind). **Failed batches**: a directory's batch was marked done even when it threw, so a space refusal left the folder permanently empty, and a second pane waiting on the same batch listed whatever was on disk. Each run is now an attempt whose outcome — success, or the error — is handed to every caller waiting on it, and only success is remembered; the next listing after a refusal tries again. Encrypted content is **not supported**, by the owner's decision (2026-09-23): there is no password prompt and no decryption, and an encrypted member stays hidden rather than being shown as an unavailable row with its reason — including once listings are built from the tree. What is kept is the exclusion itself, because removing it would bring back zeros served as a file's contents. Costs: a user browsing a mixed archive is not told that some members were left out |
